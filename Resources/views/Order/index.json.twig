{% trans_default_domain "dzangocart" %}

{% spaceless %}
{
	"sEcho": "{{ app.request.query.all.sEcho }}",
	"iTotalRecords": {{ total }},
	"iTotalDisplayRecords": {{ totalDisplay }},
	"aaData" : [
		{% for order in list %}
			{
				"DT_RowId": "{{ 'o_' ~ order.getOrderId }}",
				"DT_RowClass": "{{ order.isTest ? 'test' : '' }}",
				"id": {{ order.getOrderId }},
				"0": "",
				"1": "{{ order.getDate|localizeddate('medium', 'short', app.request.locale, null, datetime_format) }}",
				"2": "{{ order.getOrderId }}",
				"3": "{{ order.getCustomer.getName }}",
				"4": "{{ order.getCurrency }}",
				"5": "{{ order.getAmountExcl }}",
				"6": "{{ order.getTaxAmount }}",
				"7": "{{ order.getAmount }}",
				"8": "{{ order.getAmountPaid }}",
				"9": "{{ order.affiliate }}",
				"10": "{{ order.isTest }}",
				"11": ""
			}{% if not loop.last %},{% endif %}
		{% endfor %}
	]
}
{% endspaceless %}